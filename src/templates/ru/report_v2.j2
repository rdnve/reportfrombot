<b>{{ today.strftime("%d.%m.%Y") }}</b>

<i>Факт</i>{% for x in data.done %}
 » {{ x }}{{ ";" if not loop.last else "." }}{% else %}
 » Похоже, кое-кто сегодня бездельничал.{% endfor %}

<i>Горячий бэклог</i>{% for x in data.hot_backlog %}
 » {{ x }}{{ ";" if not loop.last else "." }}{% else %}
 » Задач в горячем бэклоге пока нет.{% endfor %}

 <i>Холодный бэклог</i>{% for x in data.cold_backlog %}
 » {{ x }}{{ ";" if not loop.last else "." }}{% else %}
 » Задач в холодном бэклоге не найдены.{% endfor %}

<i>{{ "Новая неделя" if is_friday else "Завтра" }}</i>{% for x in data.tomorrow %}
 » {{ x }}{{ ";" if not loop.last else "." }}{% else %}
 » Задачи ещё не перенесены.{% endfor %}

{% if data.additionally %}<i>Дополнительно</i>{% for x in data.additionally %}
 » {{ x }}{{ ";" if not loop.last else "." }}{% endfor %}{% else %}{% endif %}
